import 'package:flutter/material.dart';

class MAQView extends StatelessWidget {
  const MAQView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
